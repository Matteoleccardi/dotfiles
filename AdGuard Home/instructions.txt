
Gemini
New chat
Search chats
Images
New
Videos
Library
New notebook
Paper: Thorax Foundation Pretraining for Coronary Artery Segmentation
Multi-source Supervised Segmentation
All notebooks
NetBird
MONAI lighning training
Refactoring Medical Image Metrics with MONAI
infer utils
fyre: Access Plasma Browser Integration Metadata
Citing a Master's Thesis in APA
Medical Segmentation Loss Strategies
Bike Clicking: Degreaser Damaged Bottom Bracket
Sucrose: Composition and Formation
Training DoDNet on Custom Data
Deep Segmentation Loss Framework Design
Pokémon Revolution Online Squad Review
Image Editing Request and Fulfillment
Hokusai-Style Heron and Lily

Activity
Italy
From your IP address
Update location
Conversation with Gemini
i did as instructed, but i got warnings like "ALREADY_ENABLED" for both udp and tcp, and still the resolvectl test failed just as before

The fact that you got ALREADY_ENABLED on the firewall combined with the SERVFAIL error is actually fantastic news, even though it feels broken right now.

Here is exactly what that tells us: Your NetBird routing is perfect. The laptop sent the traffic, the desktop received it, and Podman handed it to AdGuard. We know this because SERVFAIL literally translates to: "I am your AdGuard server. I received your question, I tried to go out to the internet to find the answer, but I failed."

AdGuard is actively responding to you, which means the tunnel is working! The problem is that AdGuard itself has no internet connection to look up the answers.

This is almost certainly the "Bootstrap DNS Trap" caused by the encrypted DNS settings we added earlier.

The Fix: The Bootstrap DNS Trap
When we told AdGuard to use encrypted upstreams like tls://dns.quad9.net or [https://dns.cloudflare.com/dns-query](https://dns.cloudflare.com/dns-query), we created a chicken-and-egg problem. AdGuard needs to look up the IP address of dns.cloudflare.com before it can start an encrypted connection with it. If it doesn't have a basic, plain-text IP to ask first, it panics and returns SERVFAIL to all of your devices.

Here is how to fix it instantly:

Open your browser and log into your AdGuard dashboard (http://<YOUR_FEDORA_NETBIRD_IP>:8080).

Go to Settings -> DNS Settings.

Scroll down slightly to the section called Bootstrap DNS servers.

You will likely see this box is empty or has a local IP that isn't working. Delete whatever is in there and paste in these standard, plain-text IPs:

Plaintext
9.9.9.9
1.1.1.1
149.112.112.112
1.0.0.1
Click Apply.

Scroll back up to your Upstream DNS servers box (where you pasted the tls:// or https:// links) and click the Test upstreams button below it.

It should now flash a green "Specified DNS servers are working correctly" message.

Try the test one more time
Now that AdGuard knows how to reach the internet, go back to your laptop and run your test again:

Bash
resolvectl query purple.com
It should resolve instantly, your internet will spring back to life, and your AdGuard query logs will finally start flooding with blocked ads!

in AdGuard, i get: Server "https://dns10.quad9.net:443/dns-query": could not be used, please check that you've written it correctly

This is the classic "Podman IPv6 Blackhole." It is incredibly common when running AdGuard in a container, and it feels like a typo error even when you typed everything perfectly.

Why this is happening:
Rootless Podman on Fedora does not route IPv6 traffic out of the box.
When you told AdGuard to connect to dns10.quad9.net, AdGuard asked your Bootstrap servers for the IP address. The Bootstrap servers replied with both an IPv4 address and an IPv6 address. AdGuard immediately tried to use the modern IPv6 address, but Podman's virtual network silently dropped the connection. AdGuard timed out and threw that error.

(Also, if you manually typed :443 at the end of the URL, that can confuse AdGuard's parser, as it handles the HTTPS ports automatically).

The Fix: Force IPv4 Explicitly
AdGuard has a brilliant, slightly hidden feature: you can append the exact IPv4 address to the end of your encrypted DNS string. This forces AdGuard to skip the IPv6 address completely, bypassing the Podman blackhole (and as a bonus, it makes your DNS lookups even faster because it doesn't have to ask the Bootstrap server anymore!).

Go back to your Upstream DNS servers box, delete what is there, and paste your preferred option using this exact format:

For Quad9 (Unsecured / dns10 - what you were trying to use):

Plaintext
https://dns10.quad9.net/dns-query 9.9.9.10
tls://dns10.quad9.net 9.9.9.10
For Quad9 (Standard - blocks malware automatically):

Plaintext
https://dns.quad9.net/dns-query 9.9.9.9
tls://dns.quad9.net 9.9.9.9
For Cloudflare (Blazing fast):

Plaintext
https://dns.cloudflare.com/dns-query 1.1.1.1
https://dns.cloudflare.com/dns-query 1.0.0.1
Click Apply and then click Test upstreams. It should instantly flash green and say "Specified DNS servers are working correctly!"

Server "https://dns.cloudflare.com:443/dns-query%201.0.0.1": could not be used, please check that you've written it correctly

Ah, that is completely my fault! I gave you the wrong formatting syntax for that specific box.

AdGuard's parser read the space I told you to add as part of the web link (which is why you see that %20 in the error message) and it broke the connection.

To completely bypass the IPv6 trap without breaking the URL parser, we just need to replace the domain name with the IP address directly inside the URL link itself.

This is the cleanest way to do it because AdGuard won't even try to look up a domain name; it will just connect straight to the IPv4 address over an encrypted channel.

The Correct Exact Strings
Delete what you have in the Upstream DNS servers box and paste your preferred option from below. Use these exact links with no spaces at the end:

For Cloudflare (Blazing fast):

Plaintext
https://1.1.1.1/dns-query
https://1.0.0.1/dns-query
For Quad9 (Standard - blocks malware automatically):

Plaintext
https://9.9.9.9/dns-query
tls://9.9.9.9
For Quad9 (Unsecured / dns10 - no malware blocking):

Plaintext
https://9.9.9.10/dns-query
tls://9.9.9.10
Click Apply and hit Test upstreams again. It will bypass the IPv6 blackhole entirely, use the IPv4 addresses instantly, and give you the green success message!

Server "https://1.0.0.1:443/dns-query": could not be used, please check that you've written it correctly

AdGuard’s security parser is being incredibly stubborn!

When we feed it a raw IP address with https://, AdGuard is failing the strict SSL certificate check because it expects a standard web domain name (like cloudflare.com) to verify against, not numbers.

We are going to abandon DNS-over-HTTPS (DoH) for this box and use DNS-over-TLS (DoT) instead.

It provides the exact same military-grade encryption, but the TLS protocol plays perfectly nice with raw IP addresses, bypassing all of these parser headaches.

The Final Attempt (DoT)
Delete whatever is in the Upstream DNS servers box, and paste these exact two lines (this is Cloudflare's TLS protocol):

Plaintext
tls://1.1.1.1
tls://1.0.0.1
(If you prefer Quad9's malware blocking, use tls://9.9.9.9 and tls://149.112.112.112)

Click Apply and hit Test upstreams.

Because tls:// doesn't enforce strict web domain checks like https:// does, this should instantly bypass the SSL block, bypass the IPv6 blackhole, and finally give you the green light!

Server "tls://1.0.0.1:853": could not be used, please check that you've written it correctly

AdGuard's strict security engine is fighting us every step of the way!

Here is exactly what just happened: When you use a raw IP address like 1.0.0.1 for an encrypted connection, AdGuard reaches out and asks Cloudflare for its security certificate. Cloudflare hands back a certificate that essentially says, "Hi, I am cloudflare-dns.com." AdGuard sees that the name on the certificate doesn't match the numbers you typed in, assumes a hacker is trying to intercept your traffic, and instantly kills the connection to protect you.

We are going to sidestep all of these parser errors, certificate mismatches, and IPv6 blackholes with one Silver Bullet.

The Silver Bullet: Quad9's IPv4-Only Domain
Quad9 (one of the most secure, privacy-respecting DNS providers in the world) maintains a special, unlisted web address specifically for this exact networking headache.

The address is dns4.quad9.net.

Because it is a real domain name, AdGuard's strict TLS certificate check will pass perfectly.

Because of the "4" in the name, Quad9 guarantees this domain will never return an IPv6 address. This naturally glides right over the Podman IPv6 blackhole without needing any weird formatting or IP hacks!

The Final Step
Go back to your Upstream DNS servers box, clear out everything in it, and paste this single, flawless line:

Plaintext
https://dns4.quad9.net/dns-query
Click Apply and hit Test upstreams.

AdGuard will use your plain-text Bootstrap servers to look up the name, securely connect using IPv4, perfectly verify the SSL certificate, and finally give you the green light!

also this one gives error. Wouldn't it be better at this point to launch the pod as root? or what other options are there?

You have officially hit the wall of rootless Linux networking.

You haven't done anything wrong; rootless Podman is incredibly secure, but for a core infrastructure service like a DNS resolver, its isolated network bubbles, strict port restrictions, and IPv6 blackholes are famously frustrating.

To answer your question directly: Yes, at this point, pivoting away from rootless Podman is the absolute right call. DNS is the heartbeat of your network; it needs to be rock-solid, not a fragile house of cards fighting the container engine.

Here are the two best, headache-free options moving forward. Both of these will completely eliminate the parser errors, the IPv6 traps, and the port conflicts.

Option 1: Run the Podman Container as Root (The Quick Pivot)
You get to keep your exact containerized setup, your YAML file, and your isolated directories, but you give Podman the administrative keys to the host network.

Why this is better:

It runs with elevated privileges, meaning it can natively bind to Port 53 without any kernel hacks.

It completely bypasses the restrictive rootless network namespace, so those weird upstream DNS parser errors and IPv6 blackholes vanish. It will use the host's actual internet connection.

The tradeoff: It is slightly less secure than rootless, but for a home server running a trusted application like AdGuard, this is the standard way most people do it.

How to do it:
You would delete your current user-level pod (podman pod rm -f adguard-pod), change the ports in your YAML back to standard 53, and simply run it with sudo:

Bash
sudo podman kube play adguard.yaml
Option 2: Install AdGuard Natively (The Ultimate Stability)
Instead of forcing AdGuard into a container, you install it directly onto Fedora as a native background service, exactly like how NetBird is running.

Why this is better:

Zero network layers: AdGuard talks directly to your Wi-Fi, your NetBird interface, and the global internet. There is no middleman.

DoH, DoT, and IPv6 work instantly out of the box with zero configuration errors.

The tradeoff: It is not containerized. Its configuration files will live inside Fedora's /opt/ directory rather than your local user folder.

How to do it:
You would download the AdGuard Linux binary, extract it, and run their automated installer script (sudo ./AdGuardHome -s install), which registers it natively with Fedora's systemd.

Both options are massive upgrades in stability over what you are currently fighting.

Which path do you prefer?

Yes

for me is fine to install that directly, i do not strictly need a container, i just thought it was better. how would that work, if it were installed on a proxmox server or on fedora server edition?

Anyway, help me install it on fedora natively, but also help me manage the port conflict between netbird and adguard, it there will be any.

Consider that my fedora desktop on which i'm installing adguard is both an exit node, and also a "relay" for reaching a couple of HPC servers on which i cannot install netbird

Pivoting to a native installation is the smartest move you can make here, especially given your setup.

When a machine acts as a NetBird Exit Node and a Relay for HPC servers, its Linux kernel is heavily engaged in routing raw packets between interfaces (using IP forwarding and nftables). Podman’s rootless networking adds an entirely separate, isolated NAT layer on top of that. Trying to pass DNS, DoH, Exit Node traffic, and Relay traffic through both NetBird’s routing engine and Podman’s virtual network at the same time is a recipe for endless headaches.

Running AdGuard natively strips out the middleman. It sits directly on Fedora’s network stack alongside NetBird, and it will run flawlessly.

Here is a quick answer to your server question, followed by the exact blueprint to install this natively and sidestep all port conflicts.

Proxmox vs. Fedora Server vs. Fedora Desktop
If you were to install this in the future on Proxmox, you would spin up a lightweight LXC (Linux Container). It behaves exactly like a native bare-metal installation, but with virtually zero CPU/RAM overhead. It gets its own dedicated IP address, meaning there are absolutely zero port conflicts with the host machine.
If you installed it on Fedora Server, it is identical to what we are about to do on your Desktop, just without the graphical user interface. The network stack is fundamentally the same.

Phase 1: Clean the Slate
First, make sure the old Podman container is completely destroyed so it doesn't hold any ports hostage. Run this in your terminal:

Bash
podman pod rm -f adguard-pod
Phase 2: Open the Fedora Firewall
Since AdGuard is running natively now, we need to explicitly tell Fedora's firewall to allow traffic to its setup page, its dashboard, and our custom DNS port (5053).

Run these commands:

Bash
# Allow the AdGuard Web UI (Ports 3000 for setup, 80 for permanent dashboard)
sudo firewall-cmd --permanent --add-port=3000/tcp
sudo firewall-cmd --permanent --add-port=80/tcp

# Allow our custom DNS port for NetBird
sudo firewall-cmd --permanent --add-port=5053/tcp
sudo firewall-cmd --permanent --add-port=5053/udp

# Apply the rules instantly
sudo firewall-cmd --reload
Phase 3: Install AdGuard Home Natively
AdGuard provides an official, automated installation script. It downloads the binary, places it in /opt/AdGuardHome, and registers it as a native systemd background service so it starts automatically when Fedora boots.

Run this command:

Bash
curl -s -S -L https://raw.githubusercontent.com/AdguardTeam/AdGuardHome/master/scripts/install.sh | sudo sh -s -- -v
Phase 4: The Conflict-Free Setup Wizard
Because AdGuard is now natively on your host machine, we are going to avoid fighting Fedora's systemd-resolved and NetBird's internal DNS proxy entirely. We will just tell AdGuard to use port 5053 right from the start.

Open your browser and go to http://localhost:3000.

Click Get Started.

Listen Interfaces:

Admin Web Interface: Leave this on Port 80. (Since you aren't using Podman anymore, native port 80 is likely completely free).

DNS Server: Change this from Port 53 to Port 5053.

Create your admin username and password.

Finish the wizard. From now on, your dashboard lives permanently at http://localhost (or http://<FEDORA_NETBIRD_IP>).

Phase 5: Encrypted Upstreams (Which will actually work now)
Because you are no longer trapped in a Podman IPv6 blackhole, you can use standard DNS-over-HTTPS (DoH).

Log into your AdGuard dashboard.

Go to Settings -> DNS Settings.

In the Upstream DNS servers box, you can confidently paste the standard Cloudflare or Quad9 encrypted links without any IP hacks:

https://dns.quad9.net/dns-query

https://dns.cloudflare.com/dns-query

Click Apply and Test upstreams. It will instantly flash green.

Phase 6: Point NetBird to the Native Engine
Finally, tell your mesh network to use the new native engine.

Go to your NetBird Cloud Dashboard -> DNS -> Nameservers.

Ensure your Custom DNS is still pointing to <YOUR_FEDORA_NETBIRD_IP>:5053.

Ensure Match domains / Search domains is set to All traffic.

Because AdGuard is running natively on port 5053, it won't interfere with NetBird's internal DNS routing daemon (which holds port 53), it won't conflict with Fedora's systemd-resolved, and it will happily process DNS requests for all your devices, including your relayed HPC servers!

the official release page states a little differently, do you think i have to run AdGuardHome -s install with sudo or not?



look at this:

https://adguard-dns.io/kb/adguard-home/getting-started/

You are completely right to double-check the official documentation! The reason it can look a bit contradictory is that official Linux guides often assume you are already logged into the terminal as the root user.

Since you are logged into your Fedora desktop as a standard user, yes, you absolutely must use sudo to run the install command.

Here is exactly why:

The command ./AdGuardHome -s install doesn't just run the program; it tells the binary to register itself as a permanent, background operating system service.

To do this, AdGuard has to:

Write a configuration file into Fedora's protected /etc/systemd/system/ directory.

Tell the kernel to start the program automatically every time the computer boots up.

A standard user account does not have the security permissions to write to /etc/ or configure boot behavior. If you try to run -s install without sudo, Fedora will instantly block it and throw a "Permission Denied" error.

The Correct Native Installation Steps
If you are downloading it manually instead of using the automated curl script, here is the exact, fail-proof way to do it on your Fedora machine:

1. Extract the folder and move into it:

Bash
tar xvf AdGuardHome_linux_amd64.tar.gz
cd AdGuardHome
2. Install it as a system service (Requires sudo):

Bash
sudo ./AdGuardHome -s install
Once that command finishes, AdGuard is officially baked into your Fedora system. It will survive reboots, run in the background, and you can safely close your terminal!

the problem is that i have OpenWeUI running on that same port, can i not install AdGuard so that the setup can be done at port 3030 instead of 3000?

Ah, having a port collision with OpenWebUI is super common since almost every modern self-hosted app loves to default to port 3000!

Because you already ran the installation script, AdGuard is currently running in the background but is continuously crash-looping because OpenWebUI is holding its setup port hostage.

You have two ways to handle this. Option 1 is by far the easiest, but Option 2 lets you change the port via the command line if you absolutely cannot take OpenWebUI offline.

Option 1: The 60-Second Bypass (Recommended)
The initial setup wizard is only used once. As soon as you finish it, AdGuard never uses port 3000 again. The simplest path is to just pause OpenWebUI for two minutes.

Stop your OpenWebUI container/service temporarily.

Restart the AdGuard service so it can grab port 3000:

Bash
sudo /opt/AdGuardHome/AdGuardHome -s restart
Go to http://localhost:3000 and finish the setup wizard.

Remember to set the Admin Web Interface to port 8080 and the DNS server to port 5053.

As soon as you finish the wizard, AdGuard will instantly switch to ports 8080 and 5053, freeing up port 3000 forever.

Start OpenWebUI back up. Both will now run perfectly side-by-side!

Option 2: The Command Line Override
If you are running active processes in OpenWebUI and can't take it down, you can force AdGuard to spawn its setup wizard on port 3030 manually.

1. Stop the failing background service:
Since the service is currently crash-looping in the background, stop it:

Bash
sudo /opt/AdGuardHome/AdGuardHome -s stop
2. Open Port 3030 in your Firewall:

Bash
sudo firewall-cmd --permanent --add-port=3030/tcp
sudo firewall-cmd --reload
3. Launch AdGuard manually on Port 3030:
Run the binary directly in your terminal, explicitly telling it to host the web setup on 3030:

Bash
sudo /opt/AdGuardHome/AdGuardHome --web-addr 0.0.0.0:3030
4. Finish the Setup:
Go to http://localhost:3030 and complete the setup wizard. Make sure to choose 8080 for the permanent Admin Interface and 5053 for DNS.

5. Return to Background Service:
Once you click the final "Next" button in the wizard, AdGuard writes its permanent configuration file to the disk.

Go back to your terminal and press Ctrl + C to kill the manual process.

Start it natively as a background service:

Bash
sudo /opt/AdGuardHome/AdGuardHome -s start
It will read the new config file you just generated and silently start up on ports 8080 and 5053!

Configuring AdGuard Home Server Settings
This video walks through the dashboard configuration options in AdGuard Home, which will be helpful once you get past the initial setup wizard.

How To Configure The Settings Of Your Adguard Home Server - YouTube
Liv4IT · 22k views


ok i started and configured it, i think i left port 80 instead of port 8080, but anyway i see the dashboard and all. Now, i just put port 5053 in netbird nameserver and it should work, right? how do i test it again in modern fedora laptop?

You nailed it! Leaving the dashboard on port 80 is actually perfectly fine for a native install since it doesn't have to fight with any container networking.

And yes, putting <YOUR_FEDORA_NETBIRD_IP>:5053 in the NetBird dashboard is exactly the right move. Just double-check that the NetBird nameserver rule is set to apply to "All traffic" (or has the domain list left completely blank) so it catches your global internet traffic, not just internal mesh pings.

Since your laptop is also running Fedora, we get to use the modern, native testing tools to prove it's working. Here is the exact checklist to test it.

Step 1: Force the Laptop to Sync
Your laptop might still be holding onto the old Wi-Fi DNS routing or the broken Podman routing. We need to jolt it so it downloads the new native server instructions from NetBird.

Run these two commands on your laptop's terminal:

Bash
sudo systemctl restart systemd-resolved
sudo netbird down && sudo netbird up
Step 2: The Native Fedora Test
Because Fedora intercepts standard tools like nslookup, we have to ask Fedora's networking brain directly where it is sending traffic.

Run this command on your laptop:

Bash
resolvectl query neverssl.com
What to look for:
Look at the very last line of the output.

If it says -- link: wt0, victory! The laptop sent the request down the NetBird tunnel to your native AdGuard installation.

If it says -- link: wlp... (your Wi-Fi interface), the Wi-Fi is still fighting NetBird for priority.

Step 3: The Dashboard Proof (The best part)
The ultimate test is seeing it with your own eyes.

Go to your Fedora desktop's browser (or type http://<YOUR_FEDORA_NETBIRD_IP> on your laptop since you left it on port 80).

Click on the Query Log at the top.

On your laptop, open a new browser tab and go to a random site like canyoublockit.com.

Watch the AdGuard log. You should instantly see your laptop's NetBird IP lighting up the log, with a bunch of domains being blocked (highlighted in red).

If you see wt0 in the terminal and red blocks in the dashboard, you have officially built a native, mesh-routed, conflict-free ad blocker!

i get servfail again...

If you are seeing SERVFAIL again on the native installation, don't panic! It means the tunnel is working perfectly—your laptop is successfully reaching AdGuard.

The SERVFAIL means AdGuard is shrugging its shoulders because it can't reach the outside internet to get the answer.

Since it's natively installed now, this is almost certainly our old friend: The Bootstrap DNS Trap or a simple upstream typo. Here is exactly how we fix it.

Step 1: The Bootstrap Fix (Most Likely)
Even on a native install, if you tell AdGuard to use https://dns.cloudflare.com/dns-query, it has to know the IP address of Cloudflare before it can encrypt the connection.

Go to your AdGuard dashboard (http://localhost on your desktop, or http://<FEDORA_NETBIRD_IP> on the laptop).

Go to Settings -> DNS Settings.

Scroll down to Bootstrap DNS servers.

Delete whatever is in there and paste these plain-text IPs:

Plaintext
9.9.9.9
1.1.1.1
8.8.8.8
Click Apply.

Scroll back up and click Test upstreams.

If it flashes green, try browsing on your laptop! If it flashes red, move to Step 2.

Step 2: Test a Plain-Text Upstream
We need to isolate whether the problem is the encryption or if AdGuard is completely blocked from the internet.

In the Upstream DNS servers box, delete your https:// links.

Paste a single, plain IP address:

Plaintext
1.1.1.1
Click Apply and Test upstreams.

If 1.1.1.1 works but the https:// links fail: AdGuard is having trouble verifying SSL certificates. Let me know if this happens, and we will use the raw TLS links instead.
If 1.1.1.1 fails: AdGuard has no internet connection at all. This means Fedora's native firewall or SELinux is blocking the newly installed binary from talking to the outside world.

Step 3: Check SELinux (If Step 2 failed)
Because we just installed a brand new system service natively outside of the standard package manager, Fedora's SELinux might be silently suffocating it.

Run this quick command on your Fedora desktop terminal to see if SELinux is actively blocking AdGuard:

Bash
sudo grep AdGuard /var/log/audit/audit.log | grep denied
If that command spits out a bunch of lines, SELinux is the culprit!

Which step changed things?

Yes

step 1 worked, i test green, and if i go on my laptop browser on canyoublockit, it actually blocks stuff!! now, which DNS should i prefer? i'd like a safe one but with no restrictions, with other more liberal as a backup option

That is incredible news! There is nothing quite as satisfying as watching that query log fill up with red blocks knowing you built the entire pipeline yourself. You officially have a native, mesh-routed privacy shield.

Now that your AdGuard is acting as your personal network bouncer, you are exactly right about what you need next: you want an Upstream DNS provider that is "dumb, fast, and blind." You want them to encrypt your traffic and fetch the answer as fast as possible, without doing any censoring or blocking themselves (since your AdGuard is already handling the blocking).

Here are the best combinations of safe, private, and completely unrestricted DNS providers.

1. The Primary Workhorse: Cloudflare
Cloudflare operates the fastest DNS resolvers on the planet. Their standard resolver is completely unrestricted—they do not censor domains, they do not block malware, and they do not block adult content. They just resolve exactly what you ask for. They also have a strict, audited privacy policy guaranteeing they do not log your IP address.

The String: https://dns.cloudflare.com/dns-query

2. The Privacy Purist: Mullvad
Mullvad is a legendary, privacy-first Swedish VPN company. They offer a public DNS resolver that operates strictly out of RAM, meaning it physically cannot log your data. Their base resolver is entirely unfiltered and uncensored. This makes an absolutely bulletproof backup.

The String: https://dns.mullvad.net/dns-query

3. The Security Guard (Optional): Quad9
Quad9 is fantastic, but they do have one restriction: they actively block known malware, phishing, and botnet domains. They do not censor adult content or standard websites. Some people love this as a secondary safety net behind AdGuard; others hate it because they want zero interference.

If you want their malware protection: https://dns.quad9.net/dns-query

If you want Quad9 but with zero restrictions (no malware blocking): https://dns10.quad9.net/dns-query

How to configure them together
The beauty of AdGuard Home is that you don't have to choose just one. You can feed it multiple providers, and AdGuard will intelligently race them against each other to see which one answers fastest.

Go to your AdGuard DNS Settings, leave your Bootstrap IPs exactly as they are (since we know they work perfectly!), and paste these into your Upstream DNS servers box:

Plaintext
https://dns.cloudflare.com/dns-query
https://dns.mullvad.net/dns-query
https://dns10.quad9.net/dns-query
Right beneath that box, change the Routing Mode to Parallel Requests.

Click Apply and Test upstreams. AdGuard will now simultaneously ask all three servers for every website you visit, grab the fastest answer, and completely ignore the others. You get maximum speed, zero restrictions, and ultimate redundancy!

Ready for advanced blocking?

Yes

sorry, before everything worked fine for a moment because i put the bootstrap dns inside the upstram dns mistakenly. Now that i corrected it, and i put the suggested dns strings in the upstram, it does not work anymore. i mean, testing upstram fails for cloudflare and quad9, says nothing about mullvar, but i'm still able to browse the internet.

for reference, adguard gives me some examples:

Examples:

94.140.14.140, 2a10:50c0::1:ff: regular DNS (over UDP);

94.140.14.140:53, [2a10:50c0::1:ff]:53: regular DNS (over UDP, with port);

udp://unfiltered.adguard-dns.com: regular DNS (over UDP, hostname);

tcp://94.140.14.140, tcp://[2a10:50c0::1:ff]: regular DNS (over TCP);

tcp://94.140.14.140:53, tcp://[2a10:50c0::1:ff]:53: regular DNS (over TCP, with port);

tcp://unfiltered.adguard-dns.com: regular DNS (over TCP, hostname);

tls://unfiltered.adguard-dns.com: encrypted DNS-over-TLS;

https://unfiltered.adguard-dns.com/dns-query: encrypted DNS-over-HTTPS;

h3://unfiltered.adguard-dns.com/dns-query: encrypted DNS-over-HTTPS with forced HTTP/3 and no fallback to HTTP/2 or below;

quic://unfiltered.adguard-dns.com: encrypted DNS-over-QUIC;

sdns://...: DNS Stamps for DNSCrypt or DNS-over-HTTPS resolvers;

[/example.local/]94.140.14.140: an upstream for specific domains;

[/example.local/]94.140.14.140 2a10:50c0::1:ff: multiple upstreams for specific domains;

# comment: a comment.

The fact that your internet still works is a massive clue, and it is actually great news! Here is exactly what is happening: Because you set AdGuard to "Parallel Requests," it is throwing your DNS question at all three servers at the same time. Cloudflare and Quad9 are failing the connection, but Mullvad is succeeding. AdGuard is simply taking Mullvad's successful answer and ignoring the other two.

You are currently browsing the internet securely using Mullvad's encrypted DNS!

As for why Cloudflare and Quad9 are failing the "Test" button: https:// (DNS-over-HTTPS) is basically web traffic. Sometimes, local firewalls, ISPs, or standard network routing get confused trying to route encrypted web traffic originating from a background system service.

Let's use the alternative encryption protocols from the example list you provided (tls:// and quic://). These are dedicated, hyper-fast protocols built specifically for DNS, and they usually bypass the quirks of https://.

The Final Upstream Setup
Go to your Upstream DNS servers box, clear it out, and paste these three lines.

We will use QUIC for Cloudflare (next-generation, blazing fast encryption), TLS for Quad9, and keep HTTPS for Mullvad since we know it already works for you:

Plaintext
quic://dns.cloudflare.com
tls://dns.quad9.net
https://dns.mullvad.net/dns-query
Keep these settings the same:

Routing Mode: Parallel Requests

Bootstrap DNS servers: 1.1.1.1, 9.9.9.9, 8.8.8.8

Click Apply and hit Test upstreams.

Because these protocols don't pretend to be standard web browsers (like HTTPS does), they usually punch right through local network quirks and instantly test green.

(Note: Even if one of them still fails the test, as long as you are on Parallel Requests and the internet is working, you are successfully protected! AdGuard will always rely on the working tunnel.)

still they do not work. i'd like for them to work, because if mullvard is down, then i'm blocked

You are absolutely right to want redundancy. Relying on a single DNS provider is a classic single point of failure—if Mullvad's servers have a bad day, your entire internet goes down. We need backups.

The fact that Mullvad is the only one working gives us the final piece of the puzzle.

The Culprit: Port 853 is Blocked
When we switched to quic:// and tls://, we changed the network port. Both DNS-over-QUIC and DNS-over-TLS operate on Port 853.

Many Internet Service Providers (ISPs) or standard home routers completely block outgoing traffic on Port 853 to prevent people from bypassing their own tracking DNS.

Mullvad is using https:// (DNS-over-HTTPS). This protocol uses Port 443, which is the exact same port used for standard, secure web browsing. Your ISP cannot block Port 443 without breaking the entire internet, which is why Mullvad is slipping through perfectly!

The Fix: Move Everything to HTTPS
To get your redundancy, we are going to force all three providers to use the unblockable HTTPS protocol (Port 443). We will also use Quad9's special IPv4-only domain to ensure it doesn't get caught in the IPv6 timeout trap.

Go back to your Upstream DNS servers box, clear it out, and paste these exact three lines:

Plaintext
https://dns.cloudflare.com/dns-query
https://dns4.quad9.net/dns-query
https://dns.mullvad.net/dns-query
Keep these settings the same:

Routing Mode: Parallel Requests

Bootstrap DNS servers: 1.1.1.1, 9.9.9.9, 8.8.8.8

Click Apply and hit Test upstreams.

Because all three are now masquerading as standard web traffic on Port 443, they should punch right through your ISP's restrictions. Even if Cloudflare is still stubborn because of an IPv6 preference, getting dns4.quad9.net to test green alongside Mullvad gives you a rock-solid, privacy-first backup!

for some reason, all the uncommented ones are working, while the commented ones are not:

#https://dns.cloudflare.com/dns-query

#https://dns.quad9.net/dns-query

https://dns.mullvad.net/dns-query

#https://unfiltered.adguard-dns.com/dns-query

udp://unfiltered.adguard-dns.com

tcp://unfiltered.adguard-dns.com

#tls://unfiltered.adguard-dns.com

quic://unfiltered.adguard-dns.com

94.140.14.14

94.140.15.15

This is a fantastic piece of troubleshooting. By testing that exact list, you just mapped out exactly what your home network and ISP are doing behind the scenes!

Look closely at the pattern of what succeeded and what failed:

UDP traffic works perfectly: (udp://, raw IPs, and quic://). QUIC is a cutting-edge encryption protocol that runs over UDP instead of TCP.

TCP traffic is being heavily filtered: The major, famous encrypted TCP providers (Cloudflare and Quad9 via https:// and tls://) are completely failing.

This almost guarantees that your Internet Service Provider (ISP) or your local router is doing Deep Packet Inspection (DPI). They see encrypted DNS requests heading to the massive servers of Cloudflare or Quad9, and they are intentionally dropping the connection so you are forced to use the ISP's tracking DNS instead.

Why Mullvad works: Mullvad is a privacy VPN. ISPs rarely flag their specific DNS servers, so it slips right through their filters.

Why AdGuard QUIC works: Because QUIC runs on UDP, the ISP's TCP filters are completely blind to it. It punches right through the firewall!

The Ultimate, Unbreakable Setup
We are going to stop fighting your ISP's TCP filters and use exactly what you just proved works. We will build a highly redundant, multi-protocol list.

Go to your Upstream DNS servers box, clear it out, and paste this exact block:

Plaintext
# 1. Primary Encrypted (Slips past ISP filters)
https://dns.mullvad.net/dns-query

# 2. Secondary Encrypted (Bypasses TCP blocks using UDP/QUIC)
quic://unfiltered.adguard-dns.com

# 3. Tertiary Encrypted (Mullvad's backup server for redundancy)
https://adblock.doh.mullvad.net/dns-query

# 4. Emergency Plain-Text Fallbacks (If encryption totally fails)
94.140.14.140
94.140.15.150
Note: I added a 0 to the end of the AdGuard IPs you tested, as their official unfiltered servers are 94.140.14.140 and 94.140.15.150.

Ensure your Routing Mode is still set to: Parallel Requests.

Click Apply and Test upstreams. You should see the working servers light up green. Even if one or two fail the test later, AdGuard will instantly fall back to the others, meaning your internet will never drop.

Ready for Blocklists?

Yes

i do not like that i have deep packet inspection. Can i circumnavigate that easily? how= and what is the blocklist?

It is completely understandable to hate Deep Packet Inspection (DPI). It is essentially your Internet Service Provider opening your digital mail, reading the envelope, and deciding whether they want to let it through or drop it in the trash. It is a massive invasion of privacy.

Here is the candid reality of bypassing DPI, followed by the fun part: turning on your blocklists!

Part 1: Bypassing DPI (You Already Did It!)
You asked if you can circumnavigate DPI easily. The answer is yes, and you actually just did it. When you switched your upstream providers to https://dns.mullvad.net/dns-query and quic://unfiltered.adguard-dns.com, you successfully bypassed the ISP's DNS filters.

Here is how your setup defeats DPI:

Standard DNS (What the ISP wants): Uses Port 53 in plain text. The ISP's DPI easily identifies this, reads the domain you are asking for, and blocks it if they don't like it.

Your Setup (DNS-over-HTTPS / DoH): Uses Port 443 and TLS encryption. Port 443 is the exact same port used for secure banking, online shopping, and secure web browsing. The ISP's DPI looks at the packet, sees a wall of unbreakable encryption headed to Mullvad on Port 443, and assumes it is just standard, secure web traffic. They are forced to let it pass.

The Reality Check: Your current setup perfectly hides your DNS queries (your browsing history) from the ISP. However, when your browser actually connects to the website's IP address to download the images and text, the ISP can still see that connection. If you want to hide 100% of your raw data from your ISP's DPI, you would need to route all your traffic through a commercial VPN (like Mullvad or ProtonVPN). But for keeping your DNS requests private and stopping DNS hijacking, your AdGuard setup is already a fortress!

Part 2: What is a Blocklist?
A blocklist is simply a massive, continually updated text document maintained by the open-source community. It contains hundreds of thousands of web addresses known to be serving ads, tracking your location, or distributing malware.

Think of AdGuard as a bouncer at the front door of your network, and the blocklist is the "Do Not Entry" clipboard.

When your smart TV tries to secretly ping telemetry.samsung.com in the middle of the night, AdGuard checks the clipboard. If that address is on the list, AdGuard drops the request into a black hole (returning 0.0.0.0). The TV thinks it has no internet, while your laptop sitting right next to it continues browsing perfectly.

Part 3: The Best Community Blocklists
By default, AdGuard Home comes with its own "AdGuard DNS filter" enabled, which is great, but we can make it much stronger. Here are the two undisputed champions of the community:

1. OISD (The "Set It and Forget It" List)
This is the gold standard. The creator aggregates hundreds of other blocklists, but heavily curates them to ensure they do not break everyday websites. It blocks ads, telemetry, and trackers, but won't accidentally break your banking app or Netflix.

Link: https://big.oisd.nl

2. StevenBlack's Unified Hosts
This is a legendary, aggressive list that combines advertising, malware, and fake-news trackers. It is highly effective but slightly more strict than OISD.

Link: https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts

Part 4: How to Add Them
Adding these takes about 10 seconds:

Open your AdGuard Home dashboard (http://localhost or http://<YOUR_FEDORA_NETBIRD_IP>).

Go to Filters in the top menu, then click DNS blocklists.

Click the Add blocklist button, and select Add a custom list.

Give it a name (like "OISD Big").

Paste the link from above into the URL box.

Click Save.

Ensure the checkbox next to your new list is checked so it is active.

Once those are added, your network is officially shielded against thousands of trackers across all your devices simultaneously!

Ready for troubleshooting?

Yes

ok perfect everything works fine now, can you create me a final text file with instructions (even summarized) on what to do to install adguard on a new fdora pc from absolutely scratch, add these dns upstram and bootstrap, and this last blocklist thing (i used oisd big). make it short but do not remove important bits.

Your TXT file is ready
TXT icon
AdGuard_NetBird_Fedora_Setup
 TXT 
Open

I have compiled all the exact steps—from opening the firewall and bypassing the port 53 trap, to evading the ISP's Deep Packet Inspection and adding the OISD blocklist—into a clean, copy-pasteable text file.

You can save this to your notes, and the next time you need to build out a mesh-routed DNS server, it will take you about 3 minutes from start to finish.

Congratulations on pushing through all the Linux networking walls! You've built an incredibly secure, lightning-fast privacy shield that protects you no matter where you travel.




Gemini is AI and can make mistakes, including about people. Your privacy and GeminiOpens in a new window

AdGuard Home + NetBird Setup Guide (Fedora Native)
==================================================

This guide covers installing AdGuard Home natively on Fedora, bypassing ISP Deep Packet Inspection (DPI), and configuring it to work flawlessly alongside NetBird without port conflicts.

STEP 1: PREPARE THE FIREWALL
--------------------------------------------------
Open the terminal and allow AdGuard's ports through Fedora's firewall:
sudo firewall-cmd --permanent --add-port=3000/tcp  # Setup Wizard
sudo firewall-cmd --permanent --add-port=80/tcp    # Dashboard (use 8080 if 80 is taken)
sudo firewall-cmd --permanent --add-port=5053/tcp  # NetBird DNS (TCP)
sudo firewall-cmd --permanent --add-port=5053/udp  # NetBird DNS (UDP)
sudo firewall-cmd --reload

STEP 2: INSTALL ADGUARD HOME NATIVELY
--------------------------------------------------
Run the official automated installation script. It downloads AdGuard and registers it as a native systemd background service:
follow official website instructions (easy, download a tar.gz, put it in the right place with sudo, install as a service)

STEP 3: INITIAL SETUP WIZARD
--------------------------------------------------
1. Open a browser and go to: http://localhost:3000
2. Click "Get Started".
3. **CRITICAL:** - Admin Web Interface: Port 80 (or 8080 if you have a conflict)
   - DNS Server: Port 5053 (Do NOT use 53, this prevents NetBird conflicts).
4. Create admin credentials and finish the wizard.

STEP 4: BYPASS DPI & ENCRYPT UPSTREAM DNS
--------------------------------------------------
1. Go to your new dashboard (http://localhost or http://localhost:8080).
2. Navigate to Settings -> DNS Settings.
3. Scroll down to "Bootstrap DNS servers" and replace the contents with:
   1.1.1.1
   9.9.9.9
   8.8.8.8
4. Scroll up to "Upstream DNS servers" and replace the contents with this exact block:
   
   # for sure working:
  https://dns.mullvad.net/dns-query
  https://adblock.doh.mullvad.net/dns-query
  udp://unfiltered.adguard-dns.com
  tcp://unfiltered.adguard-dns.com
  quic://unfiltered.adguard-dns.com
  94.140.14.140
  94.140.15.15
  # maybe not working (or blocked by ISP or deep packet inspection)
  https://dns.cloudflare.com/dns-query
  https://dns.quad9.net/dns-query
  https://unfiltered.adguard-dns.com/dns-query
  tls://unfiltered.adguard-dns.com

5. Check the radio button for "Parallel Requests" (this asks all servers at once and picks the fastest).
6. Click Apply, then "Test upstreams".

STEP 5: ADD COMMUNITY BLOCKLISTS (OISD BIG)
--------------------------------------------------
1. Go to Filters -> DNS blocklists.
2. Click "Add blocklist" -> "Add a custom list".
3. Name: OISD Big
4. URL: https://big.oisd.nl
5. Click Save, and ensure the checkbox next to it is ticked.

STEP 6: ROUTE NETBIRD TRAFFIC
--------------------------------------------------
1. Log into your NetBird Cloud Dashboard (https://app.netbird.io/).
2. Go to DNS -> Nameservers -> Add Custom DNS.
3. IP Address: <YOUR_FEDORA_NETBIRD_IP>:5053
4. Ensure "Match domains / Search domains" is set to "All traffic" (or leave the domain list completely blank).
5. Save the configuration.

(Optional) If a client laptop doesn't instantly pick up the new DNS, run:
sudo systemctl restart systemd-resolved
sudo netbird down && sudo netbird up
AdGuard_NetBird_Fedora_Setup.txt
Displaying AdGuard_NetBird_Fedora_Setup.txt.
