A plugin to use key 'o' to open the terminal on the selected folder.
If not a folder, it performs the default action instead.

Paste this in your .bashrc file or equivalent to make the behavior work,
and call yazy with 'y' only:

[follow this link](bashrc.d/yazi)
